# Objective:

1. Understand the three ways to obtain a `Class` object, what information it holds and the critical difference between `getClass() == x.class` and `instanceof`

2. understand that `Class` objects are unique per type

# Problem Statement

1. write a method that obtains `Class` objects for several types using all three mechanism, then compares them

2. Also demonstrate the difference between getClass() equality and instanceof

