# Objective

1. See exactly what the compiler says when you call a method that throws a checked exception without acknowledging it


# Problem Statement

1. Write a method that calls `Class.forName()` without any `throws` declaration or try-catch.

2. Try to compile it and read the compiler error carefully.

3. Then add the `throws` declaration and observe it compiles. 
